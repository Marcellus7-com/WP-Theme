jQuery(window).on('elementor/frontend/init', () => {
    const addHandler = ($element) => {
        jQuery('body')
            .find('.lvgshop-shape-dividers')
            .each(function () {
                if (!jQuery(this).hasClass('loaded')) {
                    let divider = new dividerShapes(this);
                    divider.initPoints();
                    jQuery(this).addClass('loaded');
                }
            });
    };

    elementorFrontend.hooks.addAction(
        'frontend/element_ready/lvgshop-dividers.default',
        addHandler
    );
});


jQuery(window).on('elementor/frontend/init', () => {
    const addSectionHandler = ( $element ) => {
        var attr = $element.attr('data-lvgshop-dividers');
        if (typeof attr !== 'undefined' && attr !== false) {
            let isCol = $element.hasClass('elementor-column');
            if(isCol){
                $element.find('> .elementor-column-wrap').addClass('lvgshop-elementor-section-dividers')
                $element.find('> .elementor-column-wrap').prepend($element.attr('data-lvgshop-dividers'));
            }else{
                $element.addClass('lvgshop-elementor-section-dividers')
                $element.prepend($element.attr('data-lvgshop-dividers'));
            }
            $element.attr('data-lvgshop-dividers', '');
                    $element
                        .find('.lvgshop-shape-dividers')
                        .each(function () {
                            if (!jQuery(this).hasClass('loaded')) {
                                let divider = new dividerShapes(this);
                                divider.initPoints();
                                jQuery(this).addClass('loaded');
                            }
                        });
        }
        // $e.commands.getAll();
        // 
    };
 
    elementorFrontend.hooks.addAction( 'frontend/element_ready/section', addSectionHandler );
    elementorFrontend.hooks.addAction( 'frontend/element_ready/column', addSectionHandler );
    elementorFrontend.hooks.addAction( 'frontend/element_ready/container', addSectionHandler );
});